import 'package:flutter/material.dart';
import 'base.dart';


class EmployeesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return BaseScreen(
      title: 'Home',
      content: Center(child: Text('Home Screen')),
    );
  }
}
